import { Platform, StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
  headerContainer: {
    marginLeft: 60,
    marginBottom: 3,
    marginTop: 3,
  },
  headerText: {
    color: '#404040',
  },
});
