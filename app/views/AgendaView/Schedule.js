export default class Schedule {
  constructor(type, startDate, endDate, timestamp) {
    this.type = type;
    this.startDate = startDate;
    this.endDate = endDate;
    this.timestamp = timestamp;
  }
}
